# 素材投放次数上限配置化 — 需求文档
## 1. 概述
在 TikTok 投放（auto_put_tiktok）任务表单中，「素材单轮投放次数」与「素材累计投放次数」两个字段新增「上限」选项。上限值由产研侧配置，运营在表单中选择「上限」后不再输入具体数字，任务自动跟随产研的配置值。
## 2. 背景
现网两个字段为必填数字输入框，取值分别为 1~3 与 1~10。上限数值硬编码在前端，产研每次调整上限都需要发版，且存量任务锁死在建档时的数字上，上限调大后运营必须逐个重编辑任务才能生效。
本次将上限抽为产研可配置项，运营侧以「上限」语义选项接入，使产研调整配置后运营存量任务自动跟随，无需运营介入。
## 3. 字段变更
### 3.1 控件形态
两个字段由「必填数字输入框」改为「上限 / 自定义」二选一按钮组，选择「自定义」时才显示数字输入框。
| 选项 | 行为 |
|------|------|
| 上限 | 隐藏输入框，任务存语义值，由后端运行时读取技术配置解析为具体次数 |
| 自定义（默认） | 显示输入框，运营自行填写数字，受当前上限封顶与提交校验约束 |
### 3.2 默认值
默认选中「自定义」，输入框默认值 1，与现网行为一致。
### 3.3 上限提示
按钮组下方常驻展示当前上限值（如「单轮最多进入几个系列（跨账户合计），当前上限 3」），不随选项切换隐藏——选择「上限」时同样可见，便于运营对照产研当前配置。上限数字随配置动态渲染。
## 4. 配置化
### 4.1 配置项
| 配置项 | 含义 | 现网值 |
|--------|------|--------|
| singleRound | 素材单轮投放次数上限 | 3 |
| total | 素材累计投放次数上限 | 10 |
### 4.2 读取时机
- 打开任务表单时读取一次，用于渲染上限提示文案与输入框封顶值（`max`）
- 提交任务时重新读取，按最新配置校验自定义值是否超限，避免表单打开期间配置被调整后仍可提交超限值
### 4.3 配置调整的跟随行为
运营选择「上限」时，任务中存储的是「上限」语义而非建档时刻的具体数字，因此产研调整配置后：
| 配置变化 | 存量任务（选「上限」） | 存量任务（选「自定义」） |
|----------|------------------------|--------------------------|
| 上限调大 | 下一轮执行按新上限跑，无需运营重编辑 | 不受影响，仍按运营填写的数字跑 |
| 上限调小 | 下一轮执行按新上限跑，无需运营重编辑 | 不受影响，仍按运营填写的数字跑 |
即选择「上限」等同于将决定权交给产研配置，调大调小均跟随，不设置「只升不降」例外规则。
## 5. 提交校验
| 选中项 | 校验规则 |
|--------|----------|
| 上限 | 跳过必填校验与超限校验，直接提交 |
| 自定义 | 必填；数值不得超过当前配置的上限，超限时提示「素材单轮投放次数不能超过当前上限 N」/「素材累计投放次数不能超过当前上限 N」 |
## 6. 数据结构
### 6.1 字段值
| 选中项 | singleRoundRepeat / totalRepeatCount 取值 |
|--------|--------------------------------------------|
| 上限 | 字符串 `'limit'` |
| 自定义 | 数字字符串，如 `'1'`、`'3'` |
### 6.2 后端约定
任务存储 `'limit'` 语义值，执行时由后端读取当时的技术配置解析为具体次数。跟随行为不在前端实现，原型仅表达数据结构形态。
## 7. 影响范围
- 任务表单 → 素材配置 → 素材单轮投放次数 / 素材累计投放次数
- 该两项在「素材数据筛选 = 不限」与「= 自定义」两种模式下均可见
- 表单重置（关闭弹窗）时恢复为默认「自定义」选中态
## 8. 影响文件
| 项目 | 文件 |
|------|------|
| TikTok 投放 | index.html |