# 回传模板 — 需求文档
## 1. 概述
TikTok 自动化任务「投放配置」区新增「回传模板」字段，下拉选择，必填且无默认值。TikTok Minis（小程序）推广类型不支持选择回传模板。
## 2. 背景
回传模板决定 TikTok 流量回传策略（全回传 / 扣量 / IAA 等），待选名单由管理后台接口按 媒体 × 投放策略 × 业务类型 维度返回。当前自动化任务表单未提供回传模板选择，需补充。
## 3. 变更内容
### 3.1 投放配置区
| 项目 | 原 | 新 |
|------|-----|-----|
| 位置 | 投放配置区（`#appPromotionConfig` 之外） | 不变 |
| 字段 | 无回传模板相关字段 | 「广告系列名称后缀」之后新增「回传模板」下拉（必填，无默认） |
- 可见条件：应用推广与销量均显示；推广系列类型为 TikTok Minis 时隐藏
### 3.2 下拉数据来源
待选名单由监控接口 `POST /video/callback_plan/monitorPage` 按以下参数筛选：
| 参数 | 取值 | 说明 |
|------|------|------|
| `mediaSource` | `2` | TikTok |
| `strategy` | `1`=W2A / `2`=直投 / `4`=H5 | 由推广目标×投放策略决定 |
| `businessType` | `1`=免费 / `2`=付费 | 与任务业务类型一致 |
| `enableStatus` | `1` | 仅启用状态 |
- 推广目标=应用推广 → 投放策略固定直投，`strategy=2`
- 推广目标=销量 → 投放策略 W2A/H5，`strategy` 随投放策略联动
### 3.3 表单联动
- 切换推广目标 / 投放策略 / 业务类型时，重新请求待选名单并清空已选项
- 推广系列类型切换为 TikTok Minis 时隐藏该字段并清空已选项
### 3.4 提交
任务提交时在 `account` 级新增字段 `callbackTemplate`（回传模板 id）。
## 4. 影响文件
| 项目 | 文件 | 说明 |
|------|------|------|
| TikTok 投放 | index.html | 投放配置区新增回传模板下拉、联动逻辑、提交字段 |