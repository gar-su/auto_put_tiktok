# 账户包多账户随机创建 — 设计文档
## 1. 背景与目标
业务侧需要支持在账户包里的多个账户同时创建任务，账户数量可自定义（几个账户），与「素材重复次数」、「多轮执行不投重复素材」、「单轮单短剧计划创建上限」联动。当前任务仅单账户创建，需扩展为每轮从账户包随机抽取 N 个账户并行创建。同时素材重复配置重构为「单轮投放次数 × 累计投放次数」两个正交维度，替代「重复次数 + 跨轮去重开关」，覆盖旧全部组合并新增「每轮 1 次、累计 N 次」分散投放能力。
## 2. 概念模型
每轮执行时，从账户包 M 个账户中随机无放回抽取 N 个（N 用户自定义，`1 ≤ N ≤ M`），在这 N 个账户上并行创建。每个账户 = 同一套任务配置的独立执行实例：各自按素材筛选取素材、各自建 ≤ campaignLimit 条计划、各自遵守素材单轮投放次数与累计投放次数、各自维护「已投素材」历史。
## 3. 表单改动（投放配置）
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 账户包 | 下拉 | 是 | 保留现有 `#accountPackage`，选择账户包 |
| 每轮账户数 | 数字输入 | 是 | 新增 `#accountCount`，范围 `1~包内账户总数`，辅助文案「包内共 M 个账户，每轮随机抽取 N 个」 |
## 4. 联动项语义
| 联动项 | 多账户语义 |
|--------|-----------|
| `campaignLimit`（单轮单短剧计划创建上限） | 每账户独立 → 单轮上限总量 = N × 上限 |
| `repeatCount`（素材单轮投放次数） | 每账户独立取池、独立计数，单轮内复用次数，同一素材可跨账户复用 |
| `totalRepeatCount`（素材累计投放次数） | 每账户独立计数，累计投满即退出该账户后续轮次，被抽中账户继承自己的已投历史 |
## 5. 数据模型
- `mockAccountPackages` 增加 `accounts: [{id, name}]`，账户与 `mockProductAccounts` 对齐（带商品库与身份数据）。
- 提交字段：`account.package`（账户包名称）+ `account.accountCount`（每轮账户数）。
- `material.*` 中素材重复配置改为：`repeatCount`（单轮投放次数）+ `totalRepeatCount`（累计投放次数），移除 `dedupAcrossRounds`。
## 6. 商品库关联与身份匹配
保留「短剧 × 账户」逐账户粒度，但账户集合从「全部 mockProductAccounts」限定为「**账户包内账户**（候选池）」：
- `dramaMatches` 的账户维度 = 包内账户候选池。
- 身份匹配（4.8）校验遍历「包内账户 × 短剧」全部组合，任何账户都可能被抽中，必须全部可匹配；缺失组合以 alert 阻断并列出明细。
- 执行时对随机抽中的 N 个账户生效。
## 7. 校验与边界
- `accountCount` 范围 `1~M`，越界报错。
- 账户包无账户 → 报错。
- 商品/身份匹配需包内账户全覆盖。
- 素材预览与账户无关，保持不变。
## 8. 素材重复配置正交化
### 8.1 表单字段
| 字段 | 类型 | 默认 | 说明 |
|------|------|------|------|
| 素材单轮投放次数 | 不限 / 自定义 M | 自定义 = 1 | 单轮内最多复用 M 次 |
| 素材累计投放次数 | 不限 / 自定义 N | 自定义 = 1 | 累计最多投 N 次，投满退出 |
删除旧「素材重复次数」「多轮执行不投重复素材」两控件，新字段位于素材配置区。
### 8.2 覆盖矩阵
| 新配置 (单轮 M, 累计 N) | 等价旧行为 |
|------|------|
| (1, 1) | 旧「不限 + 去重开」= 每素材只投 1 次 |
| (N, N) | 旧「N + 去重开」= 每轮 N 次，投满 N 退出 |
| (N, 不限) | 旧「N + 去重关」= 每轮 N 次，跨轮无限复用 |
| (1, 不限) | 旧「不限 + 去重关」= 每轮 1 次，无限轮次 |
| (1, N) 新增 | 每轮 1 次、累计 N 次，分散至多 N 轮 |
### 8.3 边界
- 单轮实际次数 = min(M, N 剩余)，N 为硬上限；M > N 剩余时被截断，不视为错误。
- 累计到 N 退出仅影响该账户后续轮次，其他账户不受影响。
- 提交 `material.*`：`repeatCount`（单轮）+ `totalRepeatCount`（累计），移除 `dedupAcrossRounds`。
