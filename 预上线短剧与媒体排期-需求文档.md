# PRD-短剧选择支持预上线短剧与媒体排期参数
## 1. 概述
短剧选择部分当前仅支持选择上线状态为「已上线」的短剧。业务方需针对「预上线」状态的短剧提前创建自动化任务，因此短剧搜索需覆盖已上线与预上线两种状态（「待上线」状态不支持）；同时任务表单新增媒体侧排期参数，业务可指定排期时间，系统先创建监测链接与广告计划，到期后媒体侧自动开启投放。
## 2. 背景
预上线短剧尚未进入可投放窗口，但投放运营需提前配置自动化任务，待短剧上线后立即起量。当前短剧选择被限定为已上线短剧，无法满足提前建单诉求。注意：上线状态含「已上线 / 预上线 / 待上线」三态，本次仅支持已上线与预上线，待上线短剧不在可搜索范围内。
## 3. 短剧选择扩展
### 3.1 上线状态
- 短剧库数据含「上线状态」字段，取值：已上线 / 预上线 / 待上线
- 短剧搜索仅返回**已上线 + 预上线**，排除待上线；不做状态筛选控件
- 搜索结果卡片不展示上线状态与预计上线时间
### 3.2 搜索规则
| 模式 | 匹配字段 | 状态范围 |
|------|---------|---------|
| 按短剧搜索 | 短剧名称 | 已上线 + 预上线（排除待上线） |
| 按剧本搜索 | 剧本ID / 剧本名（可选语言） | 已上线 + 预上线（排除待上线） |
- 无命中时展示占位提示
## 4. 媒体排期参数
### 4.1 排期时间字段
任务表单新增「排期（东八区）」字段（任务级，所有短剧共用），输入形式为日期 + 具体时间，**按东八区填写**。
- 位置：执行频率之后、选择短剧之前
- 说明文案：「已选预上线短剧时必填；留空表示立即投放；时间按东八区填写」
### 4.2 必填规则
| 已选短剧状态 | 排期（东八区） |
|-------------|---------|
| 含预上线短剧 | 必填 |
| 全为已上线 | 选填，留空 = 立即投放 |
### 4.3 投放机制
- 排期时间确定后，任务创建时即生成监测链接与广告计划
- 到期时间点之前广告计划处于「待投放」状态，不消耗预算
- 到达排期时间点后，媒体侧自动开启投放
### 4.4 时区转换
- 任务排期时间以东八区（UTC+8）为基准填写，即系统统一时区
- **账户级时区**：不同投放账户可能处于不同时区，创建广告计划时，需将任务排期时间从东八区**转换为该投放账户所在时区**，转换后的时间作为该账户广告计划的媒体排期参数
- 示例：任务排期 2026-08-01 10:00（东八区），某账户时区为东五区，则该账户广告计划排期为 2026-08-01 07:00（账户时区）
## 5. 校验与提交数据格式
### 5.1 提交校验
- 已选短剧含预上线且排期时间为空 → 拦截并提示填写排期时间
### 5.2 提交数据
```json
{
  "scheduleTime": "2026-08-01T10:00+08:00",
  "dramas": [
    {"key": "L0001-繁花-Mekar-马来文", "status": "预上线", "targetingPackages": []}
  ]
}
```
- `scheduleTime`：排期时间，ISO 格式并**携带东八区时区偏移（+08:00）**；已上线短剧可缺省（null 表示立即投放）
- 后端创建广告计划时，将 `scheduleTime` 按各投放账户所在时区转换后使用（见 4.4 时区转换）
- `dramas[].status`：所选短剧的上线状态（已上线 / 预上线），用于后端区分是否触发排期逻辑
## 6. 影响范围
| 项目 | 文件 | 变更 |
|------|------|------|
| auto_put_tiktok | index.html | 新增 mockDramaLib 短剧库（含上线状态）；搜索改为从库中匹配；新增排期（东八区）字段、dramaStatusMap、提交校验与 payload 扩展 |
| auto_put_tiktok | 本需求文档 | 新增 |
## 7. 实现说明
- `mockDramaLib` 数组元素结构：`{ playbookId, playbookName, name, lang, status }`，`status ∈ {已上线, 预上线, 待上线}`，库中可含待上线条目用于验证其被排除
- 搜索匹配后按 `status !== '待上线'` 过滤，仅返回已上线与预上线
- 搜索结果卡片携带隐藏的 `data-status`，addDrama 时写入平行 `dramaStatusMap`（key 与 dramaTargetingMap 一致）
- `clearAllDramas` 同步清空 `dramaStatusMap`
- 排期时间使用原生 `datetime-local` 输入
