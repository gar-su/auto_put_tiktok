# H5 投放形态 — 需求文档
## 1. 概述
销量目标（`WEB_CONVERSIONS`）内新增 **H5 投放形态**：广告点击后进入短剧 H5 落地页（网页），区别于商品电商形态。投放策略由只读「W2A」改为可选按钮组「W2A / H5」，像素ID 两种策略均显示必填；同时销量模式移除「选择应用」，渠道号移至投放策略下方。
## 2. 背景
业务需支持 H5 落地页投放。当前销量目标投放策略固定为 W2A（只读展示），需开放为可选项。H5 形态与 W2A 的关键差异体现在监测链接创建参数（H5 对应 `promotionType=4`、`promotionChannel=2`），像素回传与付费配置一致，故像素ID 字段保留。
## 3. 变更内容
### 3.1 广告目标
广告目标按钮组不变（应用推广 / 销量），H5 为销量目标内部的投放形态选项，不新增广告目标。
### 3.2 投放策略
| 项目 | 原 | 新 |
|------|-----|-----|
| 位置 | 销量配置区，投放策略行（`#salesConfig`） | 不变 |
| 控件 | 只读文本「W2A」 | 按钮组（`#salesStrategy`），二选一 |
| 选项 | — | W2A（默认）/ H5 |
| 对应 API | `account.strategy = "W2A"` | `account.strategy = "W2A"` / `"H5"` |
- 切换投放策略不触发素材预览重置
- 提交时读取当前选中按钮组的 `data-value`
### 3.3 像素ID
像素ID 字段（`#salesPixelId`）保持不变，始终显示必填：W2A 与 H5 形态均需像素做事件回传。
### 3.4 其余字段
销量区字段顺序调整为：商品数据源、关联商品投放、业务类型、推广渠道、投放策略、渠道号、像素ID、单日预算、广告系列状态、广告组设置、广告设置。
- 移除「选择应用」：销量模式不再需要选择应用，提交 `campaign.selectApp` 一并移除（应用推广仍保留）
- 渠道号移至投放策略下方、像素ID 之前
- 投放策略切换时上述字段均不变化
### 3.5 提交数据
任务提交时，`account` 结构随投放策略取值：
| 表单控件 | 提交字段 | 说明 |
|---------|---------|------|
| 投放策略 | `account.strategy` | 销量：`W2A` / `H5`；应用推广仍为 `直投` |
| 像素ID | `account.pixelId` | 销量两种策略均携带 |
| 投放策略 | `account.monitorLink` | 新增 mock，销量时附带，见 3.6 |
| 选择应用 | `campaign.selectApp` | 销量模式移除该字段；应用推广仍保留 |
### 3.6 监测链接 mock 参数
提交数据中新增 `account.monitorLink`（mock），按投放策略带出监测链接创建关键差异字段（依据 H5 监测链接创建请求）：
| 字段 | W2A | H5 |
|------|-----|-----|
| `promotionType` | 1 | 4 |
| `promotionChannel` | 1 | 2 |
| `callbackDatas` | 沿用现有 | `["completeRegistrationCode","purchaseCode","subscribeCode"]` |
| `pixelId` | 现有值 | 现有值 |
| `channelNo` | 现有值 | 现有值 |
- H5 的 `promotionType=4`、`promotionChannel=2` 为与 W2A 的差异标识
- 回传事件与回传天数（7 天）两种形态一致
## 4. 影响文件
| 项目 | 文件 | 说明 |
|------|------|------|
| TikTok 投放 | index.html | 投放策略改按钮组、移除销量选择应用、渠道号移位、提交映射、monitorLink mock |
