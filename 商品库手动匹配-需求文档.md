# 商品库手动匹配 — 需求文档
## 1. 概述
「关联商品投放」侧边栏人工匹配交互重构：左栏已选短剧改为**逐短剧复选框**，勾选后才展示「投放账户 / 商品库 / 商品」列；每个账户独立选择商品库与商品，**点商品即生效**——将该商品写入当前账户 × 全部勾选短剧。支持在短剧列表**批量清空关联**（勾选短剧 × 全部账户）。删除原「应用到所选短剧」按钮与顶部复选框的括号文案。
## 2. 背景
业务形态为**多个本地短剧对应 TikTok 同一个商品**（同一账户内一个商品可同时覆盖多个投放短剧），需按账户分别选定商品库与商品。原「人工批量」路径存在以下问题：
- 顶部仅一个「全选短剧（匹配到本次所选商品）」复选框、无逐短剧复选框，无法圈定部分短剧作为写入范围
- 括号文案「匹配到本次所选商品」语义含混，易被误解为仅当次商品相关的短剧才参与
- 需「勾选 → 点商品 → 点『应用到所选短剧』按钮」三步才生效，操作链路冗余
- 商品列表 ✓/▣ 双标记与「每账户一个商品」的账户粒度覆盖不一致，逐短剧「选中态」已无意义
- 对已配短剧只能单条删除、无批量清空，重配成本高
## 3. 变更内容
### 3.1 短剧勾选
| 输入 | 原逻辑 | 新逻辑 |
|------|--------|--------|
| 顶部复选框文案 | 全选短剧（匹配到本次所选商品） | 全选短剧 |
| 逐短剧复选框 | 无 | 每张短剧卡片一个 ☑（勾选范围 = 点商品写入哪些短剧） |
| 全选联动 | 全选 = 批量写入范围 | 全选 = 一键勾/全不勾全部短剧，同步复选框状态 |
| 短剧卡片点击 | 进入该短剧的单短剧选中态（currentDramaName） | 切换该行勾选状态，取消单短剧选中概念 |
### 3.2 账户/商品库/商品列显隐
「投放账户 + 商品库 + 商品」列默认隐藏，勾选短剧（≥1）后自动出现，无勾选时收起并清空当前账户选择。`一键关联` 触发后自动全选短剧并展开账户列，便于逐账户复核。显隐容器为 `#plConfigArea`。
### 3.3 商品写入
| 输入 | 原逻辑 | 新逻辑 |
|------|--------|--------|
| 生效时机 | 先点商品再点「应用到所选短剧」按钮 | 点商品即生效 |
| 写入范围 | 全选复选框勾到的短剧 × 当前账户 | 逐行勾选的短剧 × 当前账户 |
| 数据语义 | dramaMatches[短剧][账户] = product | 不变，保持 dramaMatches[短剧][账户] = product |
账户间相互独立：切到另一账户后需重新选商品库与商品，互不覆盖。
### 3.4 覆盖展示
账户卡片与商品列表的 ✓ 改为**账户粒度**：勾选短剧在当前账户下统一为同一商品时，账户卡片显示 ✓ 与商品名、商品行标 ✓；不统一时账户卡片显示「已配 x/y 个短剧」。删除逐短剧选中态、▣/☑ 双标记及相关按钮。
### 3.5 批量清空关联
在左栏短剧列表新增「批量清空关联」入口，作用于**勾选短剧 × 全部账户**：
| 输入 | 逻辑 |
|------|------|
| 触发 | 短剧列表右上「批量清空关联」 |
| 范围 | 勾选的短剧，清空其在全部账户下的商品关联（dramaMatches[短剧] 整体删除） |
| 前置 | 至少勾选 1 个短剧，否则提示 |
| 确认 | confirm 弹窗，展示将被清空的勾选短剧数量 |
| 结果 | 相关账户卡片 ✓、商品行 ✓、已选商品标签同步移除；短剧卡「已匹配 x/y」归零 |
### 3.6 完整性校验
「确定」校验不变：全部已选短剧 × 全部账户均须匹配商品，**未勾选不豁免**；缺配仍提示具体短剧 → 账户。
## 4. 操作流程（新）
1. 打开「关联商品投放」抽屉，此时仅见左栏已选短剧（每行带复选框）。
2. 勾选要手动匹配的短剧（或勾顶部「全选短剧」）→ 右侧账户/商品库/商品列出现。
3. 逐个账户：点投放账户 → 选商品库 → 点对应商品，即写入该账户 × 全部勾选短剧。
4. 配错或需重配时：勾选相关短剧 → 点「批量清空关联」清除其全部账户关联，回到第 3 步重配。
5. 点「确定」提交；短剧 × 账户仍有缺配时提示补全。
## 5. 影响文件
| 项目 | 文件 | 说明 |
|------|------|------|
| TikTok 投放 | index.html | 「关联商品投放」抽屉交互重构（见下表） |
HTML 改动：
- 顶部复选框文案：全选短剧（匹配到本次所选商品）→ 全选短剧
- 第二、三栏（投放账户+商品库 / 商品）包入 `#plConfigArea`（默认隐藏）
- 删除「应用到所选短剧」按钮（`#batchAssignBtn`）
- 左栏短剧列表新增「批量清空关联」入口
JS 改动：
| 类型 | 函数/元素 | 说明 |
|------|----------|------|
| 删除 | `selectDrama` / `currentDramaName` | 单短剧选中路径取消 |
| 删除 | `batchAssignSelectedProduct` / `updateBatchAssignBtn` / `batchAssignBtn` / `batchCurrentProductId` | 「应用到所选短剧」链路整体移除 |
| 新增 | `setPlDrama` / `togglePlDrama` / `syncConfigArea` | 逐短剧勾选 + 配置列显隐 |
| 新增 | `clearSelectedMatches` | 勾选短剧 × 全部账户批量清空关联 |
| 新增 | `currentAccountProductId` / `accountMatchedDramas` | 账户粒度 ✓ 与覆盖统计 |
| 改写 | `renderDramaList` / `renderAccountListD` / `renderProductTable` / `assignProduct` | 账户粒度勾选/写入/展示 |
| 改写 | `selectAccountD` / `selectProductLibraryD` / `openProductLibraryDrawer` / `autoMatchDramas` | 去掉失效引用、补显隐联动 |
## 6. 验证
- 内联脚本 `node --check` 语法通过
- 删除标识（`currentDramaName`/`batchAssign*`/旧按钮）残留 0 处
- 需浏览器实测：勾选/全选后三列展开、点商品即写入、批量清空关联生效、切换账户独立、确定缺配提示
## 7. 备注
- 本次为纯前端原型交互调整，mock 数据与 `dramaMatches`/提交结构不变
- 「一键关联」自动匹配逻辑（按短剧名 parts[2] 匹配）不在本次范围，另行跟进
